import { createChatBotMessage } from "react-chatbot-kit";

const config = {
    botName: "LibraryBot",
  initialMessages: [createChatBotMessage(`Xin chào! Bạn cần hỗ trợ gì?`)]
}

export default config